# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(read_joint_dispatcherTaskLib)
ADD_LIBRARY(${READ_JOINT_DISPATCHER_TASKLIB_NAME} SHARED 
    ${READ_JOINT_DISPATCHER_TASKLIB_SOURCES})
add_dependencies(${READ_JOINT_DISPATCHER_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${READ_JOINT_DISPATCHER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${READ_JOINT_DISPATCHER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${READ_JOINT_DISPATCHER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${READ_JOINT_DISPATCHER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${READ_JOINT_DISPATCHER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${READ_JOINT_DISPATCHER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${READ_JOINT_DISPATCHER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${READ_JOINT_DISPATCHER_TASKLIB_HEADERS}
    DESTINATION include/orocos/read_joint_dispatcher)

